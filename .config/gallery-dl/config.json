{
  "extractor": {
    "base-directory": "$HOME/0/videos/gallery-dl",
    "cookies-from-browser": "firefox",
    //"download-archive": "$HOME/.cache/gallery-dl/archive.sqlite3",
    "retries": 6,

    "danbooru": {
      "filename": "{id}.{extension}"
    },

    "erome": {
      "filename": "{album_id}_{num}.{extension}"
    },

    "pinterest": {
      "filename": "{id}.{extension}"
    },

    "reddit": {
      "filename": "{title}_{id}.{extension}"
    },

    "realbooru": {
      "filename": "{id} - {uploader}.{extension}"
    },

    "sakugabooru": {
      "filename": "{id}.{extension}"
    },

    "twitter": {
      "filename": "{author[name]}_status_{tweet_id}_{num:>02}.{extension}"
    },

    "wallhaven": {
      "base-directory": "$HOME/0/pictures/wallpapers",
      "filename": "{id}.{extension}"
    }
  },

  "downloader": {
    "rate": "15M",
    "retries": 6
  }
}
